type TModalidade = {
  title: string;
  name: string;
  primaryColor: string;
  secondaryColor: string;
  totalNumbers: number;
  startPoin: number;
  endPoint: number;
};

export default TModalidade;

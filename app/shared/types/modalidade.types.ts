type TModalidade = {
  title: string;
  name: string;
  primaryColor: string;
  secondaryColor: string;
  totalNumbers: number;
  minNumbers: number;
  startPoin: number;
  endPoint: number;
  channel: string;
};

export default TModalidade;
